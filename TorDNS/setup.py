#!/usr/bin/env python

from distutils.core import setup

setup(name='TorDNS',
      version='1.0',
      description='Tor DNS server',
      author='Allen, Ben, Toby',
      packages=['TorDNS'],
     )
